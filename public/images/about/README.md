# About / Story page image

The Story page uses:

- **story.png** — portrait shown on the right (text on the left). Replaced with the provided asset.

To use a different image, replace `public/images/about/story.png` or point `StoryImage` at another path.
